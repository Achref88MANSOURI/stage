"""Prompt builders and output schemas for the two LLM stages (architecture
§18). One module per stage: `context_agent.py` for Stage 3, `analyst_agent.py`
(Stage 4) arrives with its own build-order step.
"""
